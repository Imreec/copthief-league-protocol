# Fixture index

**Generated by `gen_vectors.py` — do not edit.** Each fixture declares its own `status`;
`verify_vectors.py` reads it back, so a tier is stated once and cannot go stale.

What the tiers mean and what promoting one requires:
[`../docs/GOVERNANCE.md`](../docs/GOVERNANCE.md).
For the current check count, run `python verify_vectors.py` — it prints the roster and
the totals it actually ran.

| Fixture | Tier | SPEC | What it pins |
|---|---|---|---|
| [`canonical_json.json`](canonical_json.json) | `CORE` | §2 | the one canonical serialization under every hash |
| [`commit_reveal.json`](commit_reveal.json) | `CORE` | §3 | the per-step commit, plus the release's three divergent constructions |
| [`terms_signature.json`](terms_signature.json) | `CORE` | §4 | the pre-game agreement signature |
| [`game_uid.json`](game_uid.json) | `CORE` | §4 | the shared match id both peers derive without a round-trip |
| [`pheromone.json`](pheromone.json) | `CORE` | §5 | `subtractive_chebyshev_v1` emission and decay |
| [`report_consensus.json`](report_consensus.json) | `CORE` | §6 | the settlement signature's second (spaced) form, sign-then-insert |
| [`locked_model.json`](locked_model.json) | `CORE` | §7 | the locked-model doc schema and the refusal truth table |
| [`scent_book_v3.json`](scent_book_v3.json) | `PROMOTED` | §5.1 | `multiplicative_book_v1` — the book's own scent model |
| [`joint_seed.json`](joint_seed.json) | `ENH` | App. A | the joint-seed coin flip (opt-in) |
| [`derive_starts.json`](derive_starts.json) | `ENH` | App. A | seeded asymmetric starts (opt-in) |
